import React from 'react';

import { withStyles } from "@mui/styles";
import styles from './styles/AboutUsStyles';


function AboutUs(props){
 const {classes} = props;
    return (
      
      <div id ="o-nas" className={classes.AboutUs}>
       agagadgagasg
  </div>
    )
  }

export default withStyles(styles) (AboutUs);